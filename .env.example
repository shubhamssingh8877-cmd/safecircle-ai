# SafeCircle AI Environment Configuration
# Optional: Google Gemini API Key for AI Route Intelligence and dynamic safety assessments.
# If omitted, SafeCircle uses deterministic fallback safety models without failing.
VITE_GEMINI_API_KEY=
